/**
 * Closing brand mark — a giant outlined "CALA" filled with the living
 * iridescence (animated conic-gradient clipped to the text; see globals.css
 * `.kapanis .marka`).
 */
export function Closing() {
  return (
    <div className="kapanis" aria-hidden="true">
      <span className="marka">CALA</span>
    </div>
  );
}
