/**
 * Closing brand — the CALA wordmark drawn as a giant outlined serif, the last
 * breath of the page before the footer.
 */
export function Kapanis() {
  return (
    <div className="kapanis" aria-hidden="true">
      <span className="marka">CALA</span>
    </div>
  );
}
