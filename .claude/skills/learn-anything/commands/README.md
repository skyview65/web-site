# Installation

To use the `/learn` command:

1. Copy `learn.md` to your Claude Code commands directory:
   ```bash
   cp commands/learn.md ~/.claude/commands/
   ```

2. Reload Claude Code or restart your terminal

3. Use the command:
   ```
   /learn [topic]
   ```

Example: `/learn Python` or `/learn n8n automation`
